# My List simplification release .103

The owner approved candidate `796e886b1d2f05d4d72206ce8d7da089ef666853` in PR #95 after reviewing all nine normal, List tools, and expanded Combine searches screenshots at 1440, 390, and 320 pixels. The approved gallery and its README remain unchanged.

## Current-state and integration review

Fresh remote/main inspection and the served schema-2 deployment manifest both identify `f50a79c26ede9aaa78509d86902ba7c98462b79f`, runtime `2026-09-07.102`, deployment run `34184948586`. PR #95 is the existing candidate; no duplicate integration PR or release run was found. Main is already the candidate's base, so no integration merge or rebase is needed.

The release preparation changes only release tokens in `index.html`, `js/domain/clientRelease.js`, and `sw.js`, plus this record. Replacing `.103`'s release string with `.102`'s reproduces those three approved files exactly. All other runtime bytes, all nine screenshots, gallery, and review README are byte-identical to the approved commit. No new visual review is required for release-token changes.

## Code and security review scope

The local diff review covered the optional controls, filtering/selection state, independent scope union, reused compact search renderer, and clipboard failure paths. Dynamic omission names, translated labels, and copied bytes retain the existing HTML/attribute escaping. The bounded omission text includes every unresolved name without rebuilding known-variant checklists. Query serialization, priority/special identity, compact rows, grouped exports, canonical writes, provider/security/control files, and background retirement are unchanged from main. Closing Find uses the existing filter reset/debounce cancellation path. No blocking local finding was identified; independent automated code/security reviews and exact-head CI results will be recorded on PR #95 before merge.

## Qualification and evidence reuse

Approved candidate evidence: changed-area CI passed 404 contract tests (one existing skip) and 56 browser journeys (three opt-in capture skips). Additional focused local contracts passed 414 tests (one skip). The performance CI passed 31 static and six browser checks on one retry, after an initial 58 ms filter measurement against the unchanged 50 ms budget; local reproduction measured 16.7 ms. Budgets were not loosened.

Release-token qualification: `npm run check:pages-release` passed all 110 tests plus release coherence and domain/community/global identity contracts. The frozen trusted Firebase read checker passed (48 surfaces, 25 get, zero onValue). Required PR CI and guarded release validation still apply to the final source. Unchanged approved presentation evidence is reused; the gallery is not regenerated.

The older standalone session-transient harness has four documented pre-existing failures: an obsolete filter-name assertion and missing priority-state stubs. They were previously reproduced on base `f50a79c`; no historical test cleanup or additional baseline investigation is part of this release. The real browser reset check already proved Find/tools/Combine close and the filter clears. The outstanding two-account real-backend session check remains pending; synthetic frontend evidence does not complete it.

## Guarded control and rollback

The next unused runtime release is `2026-09-08.103`. The compatible existing selector is `release-pages-control-cf4502bb47f1e7e7b177b3567a080059908663a4`, dispatcher `cf4502bb47f1e7e7b177b3567a080059908663a4`, and trusted control `5ab10694abf119fc88f442c24ebd61e8c20aa8a4`. The runtime inventory equals the control's frozen inventory; no repin is required. Existing tags are immutable. The runtime tag will identify the qualified merge SHA.

Pages remains Actions-based with its required environment reviewer, administrator bypass disabled, and existing tag policy. Deployment must use the approved selector and freshly verified expected-live SHA. Inspect build provenance and digest before authorizing the pending environment deployment.

Rollback target: immutable `release-2026-09-07.102` at `f50a79c26ede9aaa78509d86902ba7c98462b79f`, using the same compatible control and exact expected-current-live guard. Rollback has not been executed.

Postdeployment verification must prove served provenance, release graph, service-worker identity, and the requested owner/public/selection/export flows using isolated synthetic data, blocking backend writes. No ordinary-user records, credentials, or accounts may be modified for testing. Final deployment and smoke evidence will be appended to the PR release report.
